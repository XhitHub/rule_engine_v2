const SIDES = ['lhs', 'rhs'];

class Controller {
  constructor(funcsMap) {
    this.funcsMap = funcsMap
  }

  addSearchForm(gRule) {
    var searchForm = {}
    SIDES.forEach(xhs => {
      resXhs = []
      gRule[xhs].map(gFact => {
  
      })
    })
  }
}
// add search form for the gRule
const addSearchForm = (gRule) => {
  var searchForm = {}
  SIDES.forEach(xhs => {
    resXhs = []
    gRule[xhs].map(gFact => {

    })
  })
}